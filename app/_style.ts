import { StyleSheet } from 'react-native';
import { colors } from './(tabs)/_utils';

export { colors };

export const styles = StyleSheet.create({
  // This file is only a placeholder route for the Tabs folder.
  // Styles are defined in app/(tabs)/index.tsx now.
  placeholder: { flex: 1, backgroundColor: colors.background },
});

export default function StylesRoute() {
  return null;
}

